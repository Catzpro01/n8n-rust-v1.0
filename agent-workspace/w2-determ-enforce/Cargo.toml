[package]
name = "determ"
version = "0.1.0"
edition = "2021"
description = "W2-DETERM-ENFORCE: RecordSet v1 record-replay determinism enforcement (agent10)"
license = "MIT"

[dependencies]
blake3 = "=1.8.7"
sha2 = "=0.10.8"

[dev-dependencies]
