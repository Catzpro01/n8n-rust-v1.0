[package]
name = "mcp"
description = "MCP native server — stdio transport, dual binding 2025/2026, tools authoring + JIT resources (W1-MCP-IMPL)."
version = "0.1.0"
edition = "2021"
license = "Proprietary"

[dependencies]
serde = { version = "=1.0.197", features = ["derive"] }
serde_json = "=1.0.114"
thiserror = "1"
