//! nodes-openapi — kontainer node hasil-generate (W3-OPENAPI-IMPL).
//! Semua tipe = tipe kernel NYATA (NodeDescriptor/ParameterSchema) — R-A agent4 #1017:
//! tidak ada vocab tipe paralel yang bocor ke runtime.

#![forbid(unsafe_code)]

pub mod generated;

pub use generated::registry;
